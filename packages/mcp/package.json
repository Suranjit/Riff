{
  "name": "@riff/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Riff MCP plugin — publish and riff on capsules from Claude Code.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "riff-mcp": "./dist/mcp.js"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@riff/shared": "workspace:*",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@riff/server": "workspace:*",
    "@types/ws": "^8.5.13"
  }
}
