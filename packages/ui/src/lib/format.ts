/** Small pure formatting helpers for the board UI. */

/** Human relative time: "just now", "3m ago", "2h ago", "5d ago". */
export function timeAgo(_thenMs: number, _nowMs: number): string {
  // TODO(#14): implement.
  throw new Error('timeAgo is not implemented yet (#14)');
}

/** Up to two uppercase initials for an avatar, e.g. "Ada Lovelace" -> "AL". */
export function initials(_name: string): string {
  // TODO(#14): implement.
  throw new Error('initials is not implemented yet (#14)');
}

/** A deterministic hue (0–359) derived from a name, for avatar coloring. */
export function nameHue(_name: string): number {
  // TODO(#14): implement.
  throw new Error('nameHue is not implemented yet (#14)');
}
